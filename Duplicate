class Solution {
    public boolean hasDuplicate(int[] nums) {
        Set<Integer>set=new TreeSet<>();
        for(int i:nums)
        {
            set.add(i);
        }
        if(set.size()!=nums.length)
        {
            return true;
        }
        return false;
        
    }
}
